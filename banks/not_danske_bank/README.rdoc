== README

1. Install all the requirements with `bundle install`
2. Run the application with `rails s`. It will start a server on `localhost:3000`
3. Create a POST request to `/createLoanRequest` with a json object as a request body and a `reply_to` parameter in the request headers.
